#include<iostream>
using namespace std;

int main() {
	int c,x,n,sum;
	cin >> c;
	while (c--) {
		cin >> n;
		sum = 0;
		for (int i = 0; i < n; ++i) {
			cin >> x;
			sum += x;
		}
		cout << sum<<endl;
	}
	return 0;
}
