#include<iostream>
using namespace std;
int main() {
	double t,a,n,max;
	cin >> t;
	while (t--) {
		cin >> n;
		max = 0;
		for (int i = 0; i < n; ++i) {
			cin >> a;
			if (a >= max) {
				max = a;
			}
		}
		printf("%.2lf\n",max);
	}
	return 0;
}
